'use strict';

/**
 * treatment-consideration service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::treatment-consideration.treatment-consideration');
