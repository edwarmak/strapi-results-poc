'use strict';

/**
 * treatment-consideration router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::treatment-consideration.treatment-consideration');
