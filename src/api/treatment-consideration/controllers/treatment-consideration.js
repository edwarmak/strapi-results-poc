'use strict';

/**
 * treatment-consideration controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::treatment-consideration.treatment-consideration');
